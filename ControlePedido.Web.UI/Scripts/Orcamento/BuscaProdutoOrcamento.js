/*
$(".clicar").click(function (evt) {
    evt.preventDefault();

    var produtoId = $(this).data("id");
    var nome = $(this).data("nome");
    var preco = $(this).data("preco").toLocaleString('pt-br', { style: 'currency', currency: 'BRL' });
    $("#hdnProdutoId").val(produtoId);
    $("#hdnPreco").val(preco);
    $("#txtResultProduto").val(nome);
    $("#txtPreco").val(preco.toLocaleString('pt-br', { style: 'currency', currency: 'BRL' }));
    $("#genericModal").modal('hide');
});
*/

